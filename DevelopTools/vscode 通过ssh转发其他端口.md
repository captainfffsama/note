比如这里想将 VNC 的端口转发到本地：

先通过 vscode 远程连接到服务器 A1

然后在服务器 A1 终端中执行 `vncserver -geometry 1920x1080 :11`，以 1080 分辨率开启 vncserver，然后通过 vnc viewer 来访问

然后再本机 ssh 中执行

```
ssh -L 0.0.0.0:5911:0.0.0.0:5911 -f huqiong@114.215.203.219 -p 40022 -N
```

即，将远端 5911 端口通过 ssh 隧道转发到本地 5911 端口